Bonjour, update QA local tenant (run 20/02 00:45) :

1) Profil/avatar restent majoritairement OK (`200/204`).
2) Régression: `GET /api/projects` retombe en `403` (plusieurs fois).
3) Upload documents: `POST /api/user/profile/documents/DRIVING_LICENSE` a encore renvoyé `500` (intermittent).
4) Le cas gros fichier doit toujours être géré en `413/400` (pas `500`).
5) Swagger `PUT /api/user/profile` reste incohérent (`profileDetails` string vs objet typé attendu).
6) Migration RBAC ADMIN doit être officielle (pas hotfix SQL local).

Merci de pousser ces correctifs backend pour clôturer.
